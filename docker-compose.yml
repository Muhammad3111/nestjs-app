services:
  app:
    build: .
    depends_on:
      exchangeApp:
        condition: service_healthy
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://postgres:postgres@exchangeApp:5432/exchangeApp # 🔄 bu yerda app → exchangeApp
      PORT: 3000
    ports:
      - '3001:3003'
  exchangeApp:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: exchangeApp # 🔄 bu yerda ham app → exchangeApp
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - '5464:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
