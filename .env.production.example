# ========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env in production and fill in real values
# NEVER commit .env file to git!

# Application
NODE_ENV=production
PORT=3001

# Database (Production PostgreSQL)
# Format: postgresql://username:password@host:port/database
MONEYCHANGE_DB_URL=postgresql://prod_user:STRONG_PASSWORD_HERE@your-db-host:5432/moneychange_prod

# For Docker Compose (if using separate variables)
POSTGRES_USER=prod_user
POSTGRES_PASSWORD=STRONG_PASSWORD_HERE
POSTGRES_DB=moneychange_prod

# JWT Secret (MUST be strong and unique in production!)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
MONEYCHANGE_JWT_SECRET=GENERATE_A_STRONG_SECRET_KEY_HERE_64_CHARACTERS_MINIMUM

# Registration Secret Key
# This is used for user registration validation
REGISTRATION_SECRET_DEFAULT=YOUR_REGISTRATION_SECRET_KEY_HERE

# CORS (comma-separated origins - your frontend URLs)
# Add all your production frontend domains
ALLOWED_ORIGINS=https://moneychange.uz,https://www.moneychange.uz,https://app.moneychange.uz

# ========================================
# SECURITY NOTES:
# ========================================
# 1. Never use default passwords in production
# 2. Use strong, randomly generated secrets
# 3. Keep this file secure and never commit to git
# 4. Rotate secrets regularly
# 5. Use environment-specific values for dev/staging/prod
